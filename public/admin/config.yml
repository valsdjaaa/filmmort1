backend:
  name: git-gateway
  branch: main
  identity: true

media_folder: "public/images"
public_folder: "/images"

collections:
  - name: "texts"
    label: "Tekstovi"
    folder: "src/content/texts"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Subtitle", name: "subtitle", widget: "string", required: false }
      - { label: "Kicker", name: "kicker", widget: "string", required: false }
      - { label: "Tag", name: "tag", widget: "select", options: ["esej", "kritika", "film"] }
      - { label: "Date", name: "date", widget: "datetime" }
      - { label: "Body", name: "body", widget: "markdown" }

  - name: "tags"
    label: "Tagovi"
    file: "src/config/tags.json"  # ← PROMENJENO
    fields:
      - label: "Tagovi"
        name: "tags"
        widget: "list"
        fields:
          - { label: "Ključ", name: "key", widget: "string" }
          - { label: "Naziv", name: "label", widget: "string" }
          - { label: "Boja", name: "color", widget: "color" }